#include <stdio.h>

int main(){
    int stNumber = 0, ndNumber = 0, sum = 0;

    scanf("%d", &stNumber);
    scanf("%d", &ndNumber);

    sum = stNumber + ndNumber;

    printf("SOMA = %d\n", sum);

    return 0;
}